#include <bits/stdc++.h>
using namespace std;

long long count(int l, int up, int n)
{
    if (l + up >= n)
        return 0;
    
    
}

void solve()
{
    string s;
    long long total = 0;
    while (cin >> s)
    {
        if (s == '0')
            return;

        int n = s.length();
        long long ans;
    }
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(nullptr);

    int t = 1;
    while (t--)
    {
        solve();
    }

    return 0;
}