#include <bits/stdc++.h>
using namespace std;

void solve()
{
    string s;
    while (cin >> s)
    {
        
    }
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(nullptr);

    int t = 1;
    while (t--)
    {
        solve();
    }

    return 0;
}